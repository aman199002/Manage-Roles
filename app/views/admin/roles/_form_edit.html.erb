<%= form_for @admin_role, :url => {:controller => "admin/roles", :action => "update" } do |f| %>
  <% if @admin_role.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@admin_role.errors.count, "error") %> prohibited this admin_role from being saved:</h2>

      <ul>
      <% @admin_role.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.label "Name" %>
    <%= f.text_field :name %><br/>
    <%= f.submit %>
  </div>
<% end %>
